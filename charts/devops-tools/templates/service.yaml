apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-tools.fullname" . }}
  labels: {{- include "devops-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "devops-tools.selectorLabels" . | nindent 4 }}
